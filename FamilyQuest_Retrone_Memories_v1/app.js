
const $=s=>document.querySelector(s),app=$('#app'),cameraInput=$('#cameraInput'),saveKey='familyquest-retrone-v1';let adventure=null,view='home',pendingMissionId=null;const defaultState={started:false,current:1,completed:[],xp:0,level:1,photos:{}};let state=loadState();function loadState(){try{return {...defaultState,...(JSON.parse(localStorage.getItem(saveKey))||{})}}catch{return {...defaultState}}}function save(){localStorage.setItem(saveKey,JSON.stringify(state))}function reset(){if(confirm('Vuoi davvero azzerare la missione?')){state={...defaultState};save();render()}}function levelForXP(xp){if(xp>=1200)return 5;if(xp>=750)return 4;if(xp>=420)return 3;if(xp>=160)return 2;return 1}function levelName(l){return['','Esploratrici','Viaggiatrici','Custodi','Archiviste','Maestre dei Ricordi'][l]}function completed(m){return state.completed.includes(m.id)}function currentMission(){return adventure.missions.find(m=>m.id===state.current)||adventure.missions.at(-1)}function missionImg(m,type='wide'){const ext={1:['png','png'],2:['png','jpg'],3:['png','png'],4:['png','png'],5:['png','png'],6:['png','png'],7:['png','png'],8:['png','png'],9:['png','png']};return `assets/missions/m${m.id}-${type}.${type==='detail'?ext[m.id][0]:ext[m.id][1]}`}function vibrate(ms=60){try{navigator.vibrate&&navigator.vibrate(ms)}catch{}}async function init(){adventure=await fetch('adventure.json').then(r=>r.json());if('serviceWorker'in navigator)navigator.serviceWorker.register('./service-worker.js').catch(()=>{});render()}function shell(content){return `<div class="topbar"><div class="brand"><div class="logo">🗝️</div><div><div class="kicker">FamilyQuest</div><b>${adventure.adventureTitle}</b></div></div><button class="btn ghost" onclick="reset()">Reset</button></div>${content}${nav()}`}function nav(){if(!state.started)return'';return `<div class="nav"><button class="${view==='mission'?'active':''}" onclick="view='mission';render()">Missione</button><button class="${view==='inventory'?'active':''}" onclick="view='inventory';render()">Inventario</button><button class="${view==='album'?'active':''}" onclick="view='album';render()">Album</button></div>`}function home(){const done=state.completed.length,pct=Math.round(done/adventure.missions.length*100);return `<section class="hero"><div class="card"><div class="hero-icon">🚲✨</div><div class="kicker">Prima avventura</div><h1 class="title">${adventure.adventureTitle}</h1><p class="subtitle">9 missioni, 9 foto, chiavi, frammenti, badge e un tesoro finale nascosto nel mondo vero.</p><div class="progress"><div class="bar" style="width:${pct}%"></div></div><p class="small">${done}/9 missioni completate · ${state.xp} XP · Livello ${state.level}</p><button class="btn" onclick="startGame()">${state.started?'CONTINUA':'START'}</button><button class="btn secondary" onclick="view='inventory';state.started=true;save();render()">Apri archivio</button></div></section>`}function startGame(){state.started=true;save();view='mission';render()}function render(){if(!adventure)return;if(!state.started){app.innerHTML=home();return}if(view==='inventory')app.innerHTML=shell(inventory());else if(view==='album')app.innerHTML=shell(album());else app.innerHTML=shell(mission())}function mission(){const m=currentMission(),done=state.completed.length,pct=Math.round(done/adventure.missions.length*100);if(done===adventure.missions.length)return finale();return `<section class="grid"><div class="card"><div class="mission-head"><div class="mission-icon">${m.icon}</div><div><div class="kicker">Missione ${m.id}/9</div><h2>${m.title}</h2></div></div><img class="mission-img" src="${missionImg(m,'wide')}" onerror="this.style.display='none'"><p class="small">${m.intro}</p><p class="clue">${m.clue}</p>${m.bonus?`<p class="small">✨ ${m.bonus}</p>`:''}<div class="actions"><button class="btn" onclick="takePhoto(${m.id})">📷 Scatta foto</button><button class="btn secondary" onclick="showTarget(${m.id})">Aiutino</button></div></div><div class="card"><div class="kicker">Avanzamento</div><h3>${pct}% completato</h3><div class="progress"><div class="bar" style="width:${pct}%"></div></div><div class="grid stats" style="margin-top:14px"><div class="stat"><b>${state.completed.length}</b><span>Missioni</span></div><div class="stat"><b>${state.xp}</b><span>XP</span></div><div class="stat"><b>${state.level}</b><span>${levelName(state.level)}</span></div><div class="stat"><b>${Object.keys(state.photos).length}</b><span>Foto</span></div></div></div></section>`}function showTarget(id){const m=adventure.missions.find(x=>x.id===id);alert(`Obiettivo da fotografare:\n\n${m.target}`)}function takePhoto(id){pendingMissionId=id;cameraInput.value='';cameraInput.click()}cameraInput.addEventListener('change',e=>{const file=e.target.files[0];if(!file||!pendingMissionId)return;const reader=new FileReader();reader.onload=()=>{state.photos[pendingMissionId]=reader.result;save();fakeScan(pendingMissionId)};reader.readAsDataURL(file)});function fakeScan(id){app.insertAdjacentHTML('beforeend',`<div class="scan"><div class="scanbox card"><div class="hero-icon">🔍</div><h2>Analisi immagine...</h2><p class="small">Confronto dettagli · verifica ricordo · sblocco archivio</p><div class="scanline"><i></i></div></div></div>`);setTimeout(()=>completeMission(id),2500)}function completeMission(id){$('.scan')?.remove();const m=adventure.missions.find(x=>x.id===id);if(!state.completed.includes(id)){state.completed.push(id);state.xp+=m.xp;state.level=levelForXP(state.xp);state.current=Math.min(id+1,adventure.missions.length+1);save();vibrate(120)}app.insertAdjacentHTML('beforeend',`<div class="reward"><div class="reward-card"><div class="reward-icon">🗝️</div><div class="kicker">Missione completata</div><h2>${m.success}</h2><p><b>${m.key}</b><br>✨ ${m.fragment}<br>🏅 ${m.badge}<br>+${m.xp} XP</p><button class="btn" onclick="document.querySelector('.reward').remove(); render()">Continua</button></div></div>`)}function inventory(){return `<section class="grid"><div class="card"><div class="kicker">Archivio</div><h2>Inventario</h2><p class="small">Livello ${state.level}: ${levelName(state.level)} · ${state.xp} XP</p></div><div class="inventory">${adventure.missions.map(m=>`<div class="item ${completed(m)?'':'locked'}"><h3>${m.icon} ${m.title}</h3><p>🗝️ ${m.key}</p><p>✨ ${m.fragment}</p><p>🏅 ${m.badge}</p></div>`).join('')}</div></section>`}function album(){const imgs=adventure.missions.map(m=>state.photos[m.id]?`<div><img src="${state.photos[m.id]}"><p class="small">${m.id}. ${m.title}</p></div>`:`<div class="item locked"><h3>${m.icon}</h3><p>${m.title}</p><p class="small">Foto non ancora scattata</p></div>`).join('');return `<section class="grid"><div class="card"><div class="kicker">Album</div><h2>Le foto della missione</h2><p class="small">Qui restano i ricordi scattati durante l'avventura.</p></div><div class="album">${imgs}</div></section>`}function finale(){return `<section class="hero"><div class="card"><div class="hero-icon">🎉</div><div class="kicker">Missione completata</div><h1 class="title">Tesoro sbloccato</h1><p class="subtitle">Avete recuperato 9 chiavi, 9 frammenti e 9 ricordi. Ora cercate la busta reale nel posto dove arrivano i messaggi.</p><div class="grid stats"><div class="stat"><b>9</b><span>Chiavi</span></div><div class="stat"><b>${state.xp}</b><span>XP</span></div><div class="stat"><b>${state.level}</b><span>Livello</span></div><div class="stat"><b>🏡</b><span>Famiglia</span></div></div><div class="actions"><button class="btn" onclick="view='album';render()">Apri album</button><button class="btn secondary" onclick="view='inventory';render()">Vedi inventario</button></div></div></section>`}init();
